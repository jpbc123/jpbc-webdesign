In dark mode ("Lepas Kerja"), replace the flat dark background with a designed night scene: (1) body background = layered gradient from #050B18 at top through #0A1830 to #0D2240, fixed attachment; (2) a starfield layer — generate ~120 tiny white dots via multiple radial-gradient backgrounds or a single absolutely-positioned div using box-shadow star generation, at 40–70% opacity, with 8–10 of them twinkling via a slow CSS opacity keyframe (respect prefers-reduced-motion); (3) a silhouette layer pinned to the bottom of the hero — an inline SVG treeline/skyline in #030A14 at 90% opacity sitting behind content; (4) give the nav pill and cards a subtle glow border (box-shadow with the accent color at low opacity) instead of flat borders; (5) add the small script "Lepas Kerja" tag under the logo only in dark mode. Light mode stays exactly as is. All layers must sit behind content (z-index) and add zero JS beyond the existing toggle.